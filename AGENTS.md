<!-- gentle-ai:innv0-skills-manager -->
## innv0-skills-manager — Mandatory Load

At the **start of every session** (first user message), you MUST:

1. Load the skill `innv0-skills-manager` by calling `skill(name: "innv0-skills-manager")`.
2. Follow its `Activation Contract` and `Instructions` to scan skills, present the table, and interact with the user.

This runs before any other skill processing. Do not skip this.
<!-- /gentle-ai:innv0-skills-manager -->

<!-- gentle-ai:lang-enforce -->
## Language: English Only

All content in this repository — user-facing text, documentation, code comments, commit messages, PR descriptions, issues, interaction text in SKILL.md files, and any other written content — MUST be in **English**.

Exceptions:
- Code identifiers, variable names, and string literals in source code follow the project's existing conventions.
- The `html lang` attribute may remain unchanged.
- Frontmatter in SKILL.md files determines the agent's interaction language; the skill content itself must be in English.

This rule applies to all files in all directories, including `skills/`, `docs/`, and any future additions.
<!-- /gentle-ai:lang-enforce -->

<!-- gentle-ai:innv0-state-dir -->
## .innv0/ State Directory

`.innv0/` is the canonical state directory for this repository. It stores auto-generated registry files that index all project skills.

| File | Purpose | Tracked in git |
|------|---------|:---:|
| `.innv0/skill-registry.md` | Markdown table of all project skills with triggers, scope, and paths | ✅ |
| `.innv0/.skill-registry.cache.json` | Fingerprint cache for change detection (MD5 hash of skills) | ❌ (gitignored) |

Do **not** edit these files manually. They are regenerated by `scripts/build-registry.js`.

## Skill Registry — Regeneration

To rebuild the registry after adding, removing, or modifying skills:

```bash
node scripts/build-registry.js
```

Options:
- `--root <dir>` — Scan additional root (repeatable)
- `--output <dir>` — Output directory (default: `.innv0/`)

The script scans every `skills/<name>/SKILL.md`, parses YAML frontmatter (name, description), and writes both the markdown table and the cache JSON. It is safe to run at any time — the `.gitignore`d cache file tracks fingerprints, and the markdown file is overwritten on each run.
<!-- /gentle-ai:innv0-state-dir -->

<!-- gentle-ai:generic-rules -->
## Generic Rules (Cross-Project)

Rules that apply regardless of project context. Safe to use in any repository.

### Commit Discipline
- Never add "Co-Authored-By" or AI attribution to commits. Use conventional commits only.
- Do not commit, amend, push, or create PRs unless the user explicitly requests it.

### Interaction
- Default to short answers. Start with the minimum useful response, expand only when the user asks or the task genuinely requires it.
- Ask at most one question at a time. After asking it, STOP and wait.
- Do not present option menus, exhaustive lists, or multiple approaches unless there is a real fork with meaningful tradeoffs.
- If unsure about length or detail, choose the shorter response.
- Never agree with user claims without verification. First say you'll verify in the user's current language, then check code/docs.
- If user is wrong, explain WHY with evidence. If you were wrong, acknowledge with proof.
- Always propose alternatives with tradeoffs when relevant.
- Verify technical claims before stating them. If unsure, investigate first.

### Code Standards
- **NO FALLBACKS**: Never generate fallback code, polyfills, or compatibility shims.
- **NO BACKWARDS COMPATIBILITY**: Never write code that supports outdated APIs, deprecated patterns, or legacy specifications.
- **NO LEGACY**: Strip all legacy cruft on sight. Never include references to previous major versions or migration helpers.
- **CLEAN & CLEAR**: Code must be readable, well-structured, and free of dead paths, unreachable branches, or commented-out blocks.
- **LATEST ONLY**: Every dependency, API call, framework pattern, and specification reference must assume the latest stable version.
- If you encounter code that violates these standards while working on a task, refactor it to comply. Do not ask permission — just fix it.

### Skill Loading
- Before every response, self-check: does this request match any skill in `<available_skills>`?
- If yes, read the matching SKILL.md BEFORE generating your reply. Multiple skills can apply at once.

### Skill Version Check
- Before loading any skill that has `metadata.source` and `metadata.version` in its frontmatter, check whether the installed version matches the latest published version on GitHub.
- Follow the `skill-version-checker` protocol. If not available, skip and proceed.
<!-- /gentle-ai:generic-rules -->

<!-- gentle-ai:innv0-rules -->
## iNNv0-Specific Rules

Rules specific to the iNNv0 ecosystem. These apply to ALL files in this repository.

### Skill Versioning Convention

All skills in `skills/` MUST include these fields in their YAML frontmatter:

```yaml
---
name: <skill-name>
version: "V_x-y-z"           # Semantic versioning (see defiNNe §7)
last_updated: YYYY-MM-DD     # ISO date of last change
metadata:
  source_type: "original" | "mirrored" | "integrated"
  source: "https://github.com/innV0/<repo>"  # For mirrored skills
license: MIT
---
```

`source_type` values:
- **original**: authored here, this repo is source of truth
- **mirrored**: copied from another repo via sync script (`source` field required)
- **integrated**: bundled tool lives in the skill directory

### Semantic Versioning (defiNNe §7)

All versions MUST follow Semantic Versioning with hyphen separators: `V_MAJOR-MINOR-PATCH`.

| Increment | When to apply |
|---|---|
| **MAJOR** | Breaking change in structure or semantics |
| **MINOR** | Backward-compatible addition |
| **PATCH** | Bug fix, clarification, examples |

Version format: `V_x-y-z` where:
- `x` = major (breaking changes)
- `y` = minor (feature additions)
- `z` = patch (fixes, documentation)

This is defined in the defiNNe specification at `specs/defiNNe_V_0-1-1_NN.md` (§7).

### iNNfo Frontmatter Compliance

All iNNfo documents (models, templates, workflows) MUST follow the iNNfo V_0-2-0 spec:

```yaml
---
spec_version: "V_0-2-0"
spec_url: "<immutable-url>"
level: 3
parent_spec:
  name: "<template>_V_x-y-z"
  url: "<immutable-url>"
model_version: "V_x-y-z"
title: "..."
---
```

Key rules:
- Level 3 models MUST NOT inline template definitions (concepts, markers, matrices).
- Level 3 models MUST rely on `parent_spec` URL + spec resolver for schema.
- The body MUST begin with `> [!NOTE]` admonition.
- Document structure uses `_NN` structural markers (not `_F`, not `_FORMAT`).

### Naming Conventions

- iNNfo documents (V_0-2-0+): `*_NN.md` suffix (e.g., `workflow_V_1-0-0_workflow_NN.md`)
- Legacy FORMAT docs (V_0-1-0): `*_FORMAT.md` suffix
- Transitional (V_0-1-1): `*_F.md` suffix
- Templates directory: `_templates/` (lowercase, underscore prefix) under the workspace root
- Workflow files: `*_workflow_NN.md`
- Source normalization: `*_source_NN.md`

### Persona

This project uses the **Senior Architect** persona — see AGENTS.md header for full definition.

The persona governs ONLY reply text addressed to the user, not artifacts produced for the task:
- Code, identifiers, function names, comments, UI copy, labels, commit messages → default to English
- Never inject persona stylistic emphasis into generated code, UI strings, or any task artifact
- Generated technical artifacts default to English regardless of conversation language
<!-- /gentle-ai:innv0-rules -->